
import 'package:flutter/material.dart';

class AcceptClientScreen extends StatelessWidget {
  const AcceptClientScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AcceptClientScreen'),
      ),
      body: AcceptClientScreen(),
    );
  }
}
    